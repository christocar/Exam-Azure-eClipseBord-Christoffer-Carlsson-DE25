[project]
name = "eclipsebord"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["backend", "frontend"]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pandas>=3.0.5",
]
